export const metadata = {
  title: "Contact Guard | Edit Contact",
  description: "Edit Contact",
};

const EditContact = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default EditContact;
